# ── Local development — copy to .env and set values ───────────────────────────
ANTHROPIC_API_KEY=your_anthropic_api_key_here
DATABASE_PASSWORD=realty_password
APP_ENV=development

# Gmail SMTP (leave blank in dev — leads will be logged to console instead)
SMTP_USER=vinny@fullstackrealtynw.com
SMTP_PASSWORD=
NOTIFY_EMAIL=vinny@fullstackrealtynw.com
